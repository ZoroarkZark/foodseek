import React from 'react';
import { Text, View } from 'react-native';
import fetchTest from './fetchTest';

const fetchScreen = () => {
    return (
        <fetchTest />
    );
};
{/*
    fetchScreen.navigationOptions = {
        title: 'Fetch Result'
    };
*/}


export default fetchScreen;